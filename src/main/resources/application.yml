#spring.application.name=UserService
spring:
  application:
    name: UserService
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: root
    url: jdbc:mysql://localhost:3306/taskmanager?createDatabaseIfNotExist=true
    username: root
  jpa:
    show-sql: true

    hibernate:
      ddl-auto: update

server:
  port: 8081